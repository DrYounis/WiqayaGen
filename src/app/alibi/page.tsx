import GeneticAlibi from '@/components/GeneticAlibi';

export default function AlibiPage() {
    return (
        <section className="bg-gradient-to-b from-white to-slate-50 min-h-screen">
            <GeneticAlibi />
        </section>
    );
}
